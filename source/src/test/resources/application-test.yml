spring:
  datasource:
    url: jdbc:tc:postgresql:15:///mtidb
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  flyway:
    enabled: true

app:
  rate-limit:
    requests-per-minute: 10000
